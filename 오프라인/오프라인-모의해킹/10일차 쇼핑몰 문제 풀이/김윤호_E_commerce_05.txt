================================
* 쇼핑몰 문제 5번
================================

1. 풀이과정

function inputCheck() {
    var inputValue = $(".inputValue").val();
    var reg = /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^&*-]).{8,}$/;
    var hangulcheck = /[ㄱ-ㅎ|ㅏ-ㅣ|가-힣]/;
    
    var encInput = encryptString(inputValue);
    $(".inputValue").val(encInput);
    return true;
}

if (true === reg.test(inputValue)) {
        inputInvalid(pwErrorarray[0]);
        return false
    } else if (/(\w)\1\1\1/.test(inputValue)) {
        inputInvalid(pwErrorarray[1]);
        return false;
    } else if (inputValue.search(/\s/) != -1) {
        inputInvalid(pwErrorarray[2]);
        return false;
    } else if (hangulcheck.test(inputValue)) {
        inputInvalid(pwErrorarray[3]);
        return false;
    }

    var encInput = encryptString(inputValue);
    $(".inputValue").val(encInput);
    return true;
이 값만 통과시키게 하면 되므로 여러가지방법이있는데 
그중에서 if문을 아에 제거해버리는 방법이나 입력값을 true로 바꿔 if문을 넘어가게함
2.정답
javascript_filtering